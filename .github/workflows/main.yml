name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
    paths-ignore:
      - '**.md'
      - 'LICENSE'
      - '.gitignore'
      - 'Source/Editor/**'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          path: Wolfrender.Blazor.Raylib

      - name: Prepare rlImGui-cs checkout path
        run: rm -rf Wolfrender.Blazor.Raylib/Library/rlImGui-cs

      - name: Checkout rlImGui-cs
        uses: actions/checkout@v5
        with:
          repository: raylib-extras/rlImGui-cs
          path: Wolfrender.Blazor.Raylib/Library/rlImGui-cs

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'
          dotnet-quality: 'preview'

      - name: Install wasm-tools workload
        run: dotnet workload install wasm-tools

      - name: Publish
        run: dotnet publish Wolfrender.Blazor.Raylib/Wolfrender.Blazor.Raylib.csproj -c Release -o output

      - name: Add CNAME for custom domain
        run: echo 'wolf3d.m0b.tech' > output/wwwroot/CNAME

      - name: Add .nojekyll
        run: touch output/wwwroot/.nojekyll

      - name: Copy index.html to 404.html for SPA routing
        run: cp output/wwwroot/index.html output/wwwroot/404.html

      - name: Setup Pages
        uses: actions/configure-pages@v6

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: output/wwwroot

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
